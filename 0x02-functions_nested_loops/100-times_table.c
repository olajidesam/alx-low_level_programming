#include "main.h"

/**
 * print_times_table - prints times table
 *
 * @n: integer number
 */
void print_times_table(int n)
{
	int i;
	int j;

	int n = 9;

	if (n > 15 || n < 0)
		printf("Nothing")

			for (i = 0; i <= n; i++)
				for (j = 0; j <= n; j++)
				{
					printf("Table");
				}
}
